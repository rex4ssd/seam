# Session Report — 2026-06-02

## 完成範圍

### Phase 3.0 — Foundation（全綠）

| 檔案 | 內容 |
|------|------|
| `core/models.py` | 加 `CloneResult`、`RepoSignals`、`StrengthReport` |
| `core/config.py` | 加 `DEFAULT_HARVEST`、`HarvestCfg` dataclass、`SeamConfig.harvest` property、`harvest_target_dir()`（target_dir 未設 → raise）、`profile_hash()`；`lang_map` 為完整 override |
| `core/store.py` | 加 `append_index_entry`、`load_index`、`harvested_keys`（`_index.jsonl` atomic append + fsync + `(candidate_id, commit)` 冪等去重）|
| `harvest/__init__.py` | 套件骨架 |
| `harvest/layout.py` | `slug_to_dir_name`、`repo_dir`、`assert_target_dir`、`ensure_lang_dir`、`write_meta`、`read_meta` |
| `tests/test_harvest_foundation.py` | 23 tests ✅ |

### Phase 3.1 — Clone engine（全綠）

| 檔案 | 內容 |
|------|------|
| `harvest/cloner.py` | `clone_repo()` / `clone_repos_batch()`；P-H01 disk guard、P-H02 timeout、P-H03 exists→fetch+reset、P-H04 `GIT_LFS_SKIP_SMUDGE`；clone 到 tmp 再 atomic rename |
| `commands/harvest.py` | `seam harvest` CLI（`--dry-run`、`--repo`、`--pipe-vein`、`--json`、`--days`）|
| `cli.py` | 加入 `cmd_harvest` |
| `pyproject.toml` | 加 `network` marker；預設 `-m 'not network'` |
| `tests/test_harvest_cloner.py` | 13 unit tests ✅（2 network tests 預設略過）|

**今日 tests：36 passed, 2 deselected**

---

## 待辦（下次開工）

| Phase | 內容 |
|-------|------|
| **3.2** | `harvest/signals.py` — 純靜態掃描：tests/、CI workflow、linter cfg、LICENSE、CHANGELOG、semver tag、dep manifest、lang breakdown、SLOC、README 長度 |
| **3.3** | `harvest/analyzer.py` — signals + 取樣檔 → ollama → 5 維分數 + tags + summary + evidence；heuristic fallback |
| **3.4** | `harvest/report.py` + `harvest/veinout.py` — 寫 STRENGTH.md、`_index.jsonl`、`--pipe-vein` 輸出 |
| **3.5** | `seam_harvest_entry.py` — 一次性 orchestration entry（cowork schedule 觸發）|

---

## 架構不變項（提醒）

- `seam harvest --dry-run` 永遠不動磁碟
- `_index.jsonl` 去重鍵 = `(candidate_id, commit)`
- `lang_map` 為完整 override，不 merge 預設
- clone 路徑：`<target_dir>/<lang>/<owner>__<repo>/`
- 化驗永遠純靜態，絕不執行 clone 下來的程式碼（P-H07）
